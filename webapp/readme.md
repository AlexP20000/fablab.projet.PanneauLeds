# Installation
- Put the webapp directory inside `/opt/`
- Put the systemd service file, `webapp.service`, inside `/etc/systemd/system/`
- `sudo systemd daemon-reload`
- `sudo systemd enable webapp`
- Reboot the system
